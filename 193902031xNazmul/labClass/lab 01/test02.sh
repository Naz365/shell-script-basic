#! /bin/bash

echo "Creating"
if [ condition ]; then
      # if body
elif [ condition ]; then
      # else if body
else
      # else body
fi
for((i=0;i<n;i++)); do
     for((j=0;j<m;j++)); do
          echo "${i}, ${j}"
     done
done
