#! /bin/bash


# num=193902031

# cnt='unset'


#  tr ' ' '\n' < file| sort | uniq -c | while read cnt num; do printf 'The number %s appears %s times in the file\n' "$num" "$cnt"; done
# while read line
#     do
#             if [ "${line}" -lt "450" ]
#             then
#                     count=$((count+1))
#             fi
#     done < file.txt

#     echo ${count}
    